const path = require('path') //Serve para resolver os caminhos


module.exports = {
    config: 
        path.resolve(__dirname, 'src', 'config', 'database.js'), //Encontrando o banco de dados
    "migrations-path": //Onde ficarão nossas migrações, ou, onde será criado a pasta migrations
        path.resolve(__dirname, 'src', 'database', 'migrations'),
    
    "seeders-path":
        path.resolve(__dirname, 'src', 'database', 'seeders'),
        
}